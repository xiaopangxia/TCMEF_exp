收藏查看我的收藏0有用+1已投票0GetClassName编辑锁定本词条缺少名片图，补充相关内容使词条更完整，还能快速升级，赶紧来编辑吧！GetClassName，函数名，该函数获得指定窗口所属的类的类名。中文名GetClassName头文件winuser.h库文件user32.libUnicodeWindows NT实现为Unicode和ANSI目录1函数信息▪函数功能▪函数原型▪参数▪返回值▪其它资料2C#声明3VB声明GetClassName函数信息编辑GetClassName函数功能该函数获得指定窗口所属的类的类名。GetClassName函数原型int GetClassName(HWND hWnd, LPTSTR IpClassName, int nMaxCount)；GetClassName参数hWnd：窗口的句柄及间接给出的窗口所属的类。IpClassName:指向接收窗口类名字符串的缓冲区的指针。nMaxCount：指定由参数lpClassName指示的缓冲区的字节数。如果类名字符串大于缓冲区的长度，则多出的部分被截断。GetClassName返回值如果函数成功，返回值为拷贝到指定缓冲区的字符个数：如果函数失败，返回值为0。若想获得更多错误信息，请调用GetLastError函数。GetClassName其它资料速查：Windows NT：3.1以上版本：Windows：95以上版本；Windows CE1.0以上版本；头文件：winuser.h库文件：user32.lib;Unicode：在 Windows NT上实现为 Unicode和 ANSI两种版本。GetClassNameC#声明编辑/// <summary>/// 该函数获得指定窗口所属的类的类名/// </summary>/// <param name="hWnd">窗口的句柄及间接给出的窗口所属的类</param>/// <param name="nIndex">指向接收窗口类名字符串的缓冲区的指针</param>/// <param name="dwNewLong">指定由参数lpClassName指示的缓冲区的字节数。如果类名字符串大于缓冲区的长度，则多出的部分被截断</param>/// <remarks>/// 速查：Windows NT：3.1以上版本：Windows：95以上版本；Windows CE1.0以上版本；/// 头文件：winuser.h/// 库文件：user32.lib;/// Unicode：在 Windows NT上实现为 Unicode和 ANSI两种版本。/// </remarks>[DllImport("User32.dll", CharSet = CharSet.Auto)]public static extern int GetClassName(IntPtr hWnd, out STRINGBUFFER ClassName, int nMaxCount);GetClassNameVB声明编辑Private Declare Function GetClassName Lib "user32" Alias "GetClassNameA" (ByVal hwnd As Long, ByVal lpClassName As String, ByVal nMaxCount As Long) As Long